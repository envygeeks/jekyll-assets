version: "2"
plugins:
  bundler-audit: { enabled:  true }
  duplication:   { enabled: false }
  rubocop:
    channel: rubocop-0-51
    config: { file: .rubocop.yml }
    enabled: true
ratings:
  paths:
  - "spec/tests/**.rb"
  - "lib/**.rb"
